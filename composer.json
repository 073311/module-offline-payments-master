{
    "config": {
        "autoloader-suffix": "offlinePayments"
    },
    "name": "ievtds/module-offline-payments",
    "description": "Offline Payments app for ievtds",
    "homepage": "https://ievtds.com",
    "keywords": [
        "laravel",
        "accounting",
        "invoice",
        "ievtds",
        "offline-payments"
    ],
    "license": "GPL-3.0+",
    "scripts": {
        "test": [
            "composer install --prefer-dist --no-interaction --no-scripts --no-suggest --no-progress --no-ansi"
        ]
    }
}
